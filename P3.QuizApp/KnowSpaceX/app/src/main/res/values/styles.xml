<resources
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>
    </style>

    <style name="questionNumber" parent="TextAppearance.AppCompat.Headline" >
        <!-- Basic Properties for the Question Numbers in the app -->
        <item name="android:textSize">22sp</item>
    </style>

    <style name="questionText" parent="TextAppearance.AppCompat">
        <!-- Properties for the Question Text in the app for consistency -->
        <item name="android:textSize">18sp</item>
        <item name="android:color">@android:color/black</item>
    </style>

    <style name="CardViewMod" parent="CardView">
        <!-- Style for the Cards in the ScrollView -->
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_marginLeft">15dp</item>
        <item name="android:layout_marginRight">15dp</item>
        <item name="android:layout_marginTop">15dp</item>
        <item name="cardElevation">10dp</item>
        <item name="android:padding">10dp</item>
        <item name="cardCornerRadius">5dp</item>
    </style>

    <style name="checkBox">
        <item name="android:textSize">18sp</item>
    </style>

</resources>
